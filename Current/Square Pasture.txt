#include <iostream>
#include <algorithm>
using namespace std;
struct coord{
    int x;
    int y;
};
struct rect{
    coord ll;
    coord ur;
};
int main() {
    int ans = 0;
    rect b1;
    rect b2;
    cin >> b1.ll.x >> b1.ll.y >> b1.ur.x >> b1.ur.y;
    cin >> b2.ll.x >> b2.ll.y >> b2.ur.x >> b2.ur.y;
    int arr[4];
    arr[0] = abs(b1.ur.x - b2.ll.x);
    arr[1] = abs(b1.ur.y - b2.ll.y);
    cout << abs(arr[1]);
    
} 